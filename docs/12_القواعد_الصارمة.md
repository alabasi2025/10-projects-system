# 📜 القواعد الصارمة (Strict Rules)

هذا المستند يحدد القواعد الإلزامية التي يجب على جميع المطورين اتباعها لضمان جودة وتوحيد العمل في جميع أنظمة المشروع.

---

### 1. قواعد البنية والهيكل (Architecture Rules)

| القاعدة | الوصف |
|---|---|
| **فصل المستودعات** | كل نظام من الأنظمة العشرة يجب أن يكون في مستودع Git منفصل. لا يُسمح بدمج أنظمة أو إنشاء مستودعات جديدة بدون موافقة. |
| **قاعدة بيانات واحدة** | جميع الأنظمة تتصل بقاعدة بيانات PostgreSQL واحدة مركزية. |
| **بادئة الجداول** | كل نظام يستخدم بادئة خاصة به لأسماء الجداول (e.g., `core_`, `asset_`, `bill_`). |
| **UUID للمفاتيح** | جميع المفاتيح الأساسية (Primary Keys) يجب أن تكون من نوع `UUID`. يمنع استخدام `Auto-increment` أو `Serial`. |

---

### 2. قواعد التقنيات (Technology Stack Rules)

| القاعدة | الوصف |
|---|---|
| **TypeScript فقط** | يمنع استخدام JavaScript. لغة TypeScript إلزامية في كل من الواجهة الأمامية والخلفية. |
| **NestJS للـ Backend** | يجب استخدام إطار العمل NestJS لبناء جميع الواجهات الخلفية (APIs). يمنع استخدام Express.js مباشرة. |
| **Angular للـ Frontend** | يجب استخدام إطار العمل Angular لبناء جميع الواجهات الأمامية. يمنع استخدام React, Vue, أو أي إطار عمل آخر. |
| **Prisma للـ ORM** | يجب استخدام Prisma كـ ORM للتفاعل مع قاعدة البيانات. يمنع استخدام TypeORM, Sequelize, أو أي ORM آخر. |
| **PrimeNG للمكونات** | يجب استخدام مكتبة PrimeNG لمكونات الواجهة الأمامية. يمنع استخدام Angular Material, Bootstrap, أو مكتبات أخرى. |
| **Tailwind CSS** | يجب استخدام Tailwind CSS مع SCSS للتنسيق. |

---

### 3. قواعد الكود والتسمية (Coding & Naming Conventions)

| القاعدة | الوصف |
|---|---|
| **`snake_case` للجداول** | أسماء الجداول والحقول في قاعدة البيانات يجب أن تكون بصيغة `snake_case` (e.g., `core_users`, `first_name`). |
| **`camelCase` للكود** | أسماء المتغيرات والدوال في TypeScript يجب أن تكون بصيغة `camelCase` (e.g., `userId`, `getCustomer`). |
| **`PascalCase` للـ Classes** | أسماء الـ Classes, Interfaces, Enums, و Types يجب أن تكون بصيغة `PascalCase` (e.g., `UserService`, `CustomerController`). |
| **`kebab-case` للملفات** | أسماء الملفات يجب أن تكون بصيغة `kebab-case` (e.g., `user.service.ts`, `customer.controller.ts`). |

---

### 4. قواعد بناء الشاشات (UI Development Rules)

| القاعدة | الوصف |
|---|---|
| **لا شاشات بدون Backend** | أي شاشة جديدة يجب أن تكون متصلة فعلياً بـ API يعمل. يمنع بناء شاشات معزولة. |
| **لا بيانات وهمية (Mock Data)** | ممنوع استخدام بيانات تجريبية أو وهمية في الشاشات. البيانات يجب أن تأتي من الـ API مباشرة. |
| **اتصال حقيقي بقاعدة البيانات** | الـ Backend API يجب أن يقرأ ويكتب من قاعدة بيانات PostgreSQL فعلياً. |
| **التحقق قبل التسليم** | كل شاشة تُختبر بإدخال بيانات حقيقية والتأكد من حفظها وعرضها بشكل صحيح. |
| **CRUD كامل** | لا تُعتبر الشاشة مكتملة إلا بعد عمل وظائف الإنشاء، القراءة، التحديث، والحذف (CRUD) بشكل كامل وصحيح. |
| **رسائل الخطأ حقيقية** | رسائل الخطأ يجب أن تأتي من الـ Backend وليست رسائل ثابتة في الـ Frontend. |
| **حالات التحميل (Loading States)** | يجب عرض مؤشر تحميل (Spinner/Skeleton) أثناء جلب البيانات من الـ API. |
| **معالجة الأخطاء (Error Handling)** | يجب معالجة أخطاء الاتصال بالـ API وعرض رسالة واضحة للمستخدم. |

**تسلسل بناء الشاشة الإلزامي:**

1.  إنشاء الجدول في `schema.prisma`.
2.  تشغيل `prisma migrate dev` لإنشاء الجدول في PostgreSQL.
3.  إنشاء NestJS `Module`, `Service`, `Controller`.
4.  اختبار الـ API عبر Postman أو Swagger.
5.  إنشاء Angular `Module`, `Component`, `Service`.
6.  ربط الـ Component بالـ API Service.
7.  اختبار الشاشة بشكل كامل ببيانات حقيقية.

---

### 5. قواعد التكامل بين الأنظمة (Integration Rules)

| القاعدة | الوصف |
|---|---|
| **التواصل عبر APIs فقط** | يمنع منعاً باتاً استدعاء قاعدة بيانات نظام آخر مباشرة. التكامل يتم حصراً عبر استدعاء APIs. |
| **JWT موحد** | النظام الأم (01) هو المسؤول الوحيد عن إصدار JWT. الأنظمة الأخرى تتحقق من صحة الـ Token فقط. |
| **نظام الأحداث (Event-Driven)** | الأحداث المهمة التي تؤثر على أكثر من نظام (e.g., `customer.created`) يجب أن تُنشر عبر نظام المطور (02). |
| **لا Foreign Keys بين الأنظمة** | الربط بين جداول تابعة لأنظمة مختلفة يتم عبر حفظ الـ UUID فقط، بدون إنشاء `FOREIGN KEY` constraint على مستوى قاعدة البيانات. |

---

### 6. قواعد المحاسبة والمالية (Accounting & Finance Rules)

| القاعدة | الوصف |
|---|---|
| **القيد المزدوج إلزامي** | كل عملية مالية يجب أن تنتج قيد محاسبي مزدوج (مدين ودائن) في النظام الأم. |
| **لا حذف فعلي** | يمنع حذف أي سجلات مالية (فواتير، قيود، سندات). يتم استخدام `Soft Delete` فقط. |
| **سجل تدقيق كامل (Audit Trail)** | يجب تسجيل كل عملية إنشاء، تعديل، أو حذف (soft) على السجلات المالية مع `userId` و `timestamp`. |
| **الفترات المحاسبية المغلقة** | يمنع تعديل أي سجلات مالية تنتمي لفترة محاسبية مغلقة. |

---

### 7. قواعد الأمان (Security Rules)

| القاعدة | الوصف |
|---|---|
| **RBAC إلزامي** | كل endpoint في الـ API يجب أن يكون محمياً بصلاحية محددة (Role-Based Access Control). |
| **تشفير كلمات المرور** | يجب استخدام `bcrypt` لتشفير كلمات المرور. |
| **HTTPS إلزامي** | يجب استخدام HTTPS في بيئة الإنتاج. |
| **التحقق من المدخلات** | يجب استخدام `class-validator` و `class-transformer` في جميع DTOs. |
| **Rate Limiting** | يجب تطبيق حد أقصى للطلبات (Rate Limiting) على جميع APIs. |

---

### 8. قواعد الجودة (Quality Rules)

| القاعدة | الوصف |
|---|---|
| **Unit Tests إلزامية** | يجب كتابة Unit Tests للـ Business Logic في NestJS Services، بنسبة تغطية لا تقل عن 80%. |
| **ESLint بدون أخطاء** | لا يُقبل أي كود يحتوي على أخطاء أو تحذيرات من ESLint. |
| **Code Review إلزامي** | لا يتم دمج أي Pull Request إلى `main` بدون مراجعة والموافقة من شخص آخر على الأقل. |
| **توثيق APIs** | يجب توثيق جميع APIs باستخدام Swagger/OpenAPI بشكل كامل وواضح. |

---

### 9. قواعد النشر والتشغيل (Deployment & Operations Rules)

| القاعدة | الوصف |
|---|---|
| **Docker إلزامي** | كل نظام (API + Web) يجب أن يعمل داخل Docker Container. |
| **متغيرات البيئة (Env Vars)** | يمنع وضع أي معلومات حساسة (Secrets) في الكود. يتم استخدام متغيرات البيئة فقط. |
| **Health Checks** | كل API يجب أن يوفر `GET /api/v1/health` endpoint. |
| **Logging موحد** | جميع السجلات (Logs) يجب أن تكون بصيغة JSON. |
